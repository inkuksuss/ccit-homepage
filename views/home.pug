extends layouts/main
include mixins/boardBlock

block content
     .boards
        each item in boards
            +boardBlock({
                id:item.id,
                title:item.title,
                views:item.views,
                boardFile:item.fileUrl
            })